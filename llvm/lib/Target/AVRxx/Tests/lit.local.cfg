import lit

# Set up the test suite for the TableGen target descriptions.
# The test files are themselves TableGen with lit commands mixed in.
config.name        = "AVRxx TableGen Target Description"
config.test_format = lit.formats.ShTest("0")
config.suffixes    = [".tdtest"]
